# distribution_gen

Generate and plot a strictly decreasing points distribution.

Installation
```
python -m pip install --upgrade pip --user
pip install -r requirements.txt --user
```

Build
```
pip install build --user
python -m build
```

Run
```
python distribution_gen.py
# or
python -m distribution_gen
```

pip install -e .
calc-results --poll-file json_poll/poll_623faac4-6c5a-4f21-9a11-4721fa0e1120.json --results-file live_results/results.json

python -m pytest -q