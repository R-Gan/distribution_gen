[metadata]
name = distribution_gen
version = 0.1.0
description = Generate and plot a strictly decreasing points distribution

[options]
packages = find:
install_requires =
    matplotlib>=3.0
python_requires = >=3.8
